# CLI Test Document

---
tags: test, cli
status: draft
---

This document was created via the **cstore CLI** tool.

## Purpose

Testing that the `cstore new` command correctly handles:

- Title (as H1 heading)
- Frontmatter (tags, status)
- Markdown content
